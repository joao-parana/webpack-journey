# webpack-journey

```bash
node -v
rm -rf /usr/local/Cellar/node/
sudo npm install -g npm 
echo $PATH | grep "/usr/local/bin"
npm init -y
id -gn $USER
echo $USER
ls -la /Users/joao_parana/.config
sudo chown -R $USER:$(id -gn $USER) /Users/joao_parana/.config
cd webpack-journey
npm init -y
npm install webpack webpack-cli --save-dev
```



